package selenium;

import java.util.Set;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;

public class SeleCode2 {

	public static void main(String[] args) throws Throwable {
		WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		
		driver.get("https://www.flipkart.com/");
		WebElement mouseHower = driver.findElement(By.xpath("//span[text()='Login']"));
		mouseHower.click();
		
		WebElement loginCreds = driver.findElement(By.xpath("//input[@class='r4vIwl BV+Dqf']"));
		loginCreds.sendKeys("12345678901");
		
		WebElement submitButton = driver.findElement(By.xpath("//button[@class='QqFHMw twnTnD _7Pd1Fp']"));
		submitButton.click();
		
		Thread.sleep(3000);
		
		driver.navigate().back();
		Thread.sleep(3000);
		WebElement loginPopup = driver.findElement(By.xpath("//span[text()='✕']"));
		loginPopup.click();
		
		Actions action = new Actions(driver);
		WebElement searchBar = driver.findElement(By.name("q"));
		searchBar.click();
		action.sendKeys("mackbookair");
		action.sendKeys(Keys.ENTER).perform();
		
		JavascriptExecutor js= (JavascriptExecutor)driver;
		js.executeScript("window.scrollBy(0,4000)");
		WebElement clickOnItems = driver.findElement(By.xpath("//div[text()='Apple 2022 MacBook AIR Apple M2 - (8 GB/256 GB SSD/Mac OS Monterey) MLXW3HN/A']"));
		clickOnItems.click();
		
		Thread.sleep(3000);
		
		String mainWindowHandle = driver.getWindowHandle(); // Store the current window handle
		Set<String> windowHandles = driver.getWindowHandles(); // Get all window handles
		for (String handle : windowHandles) {
		    if (!handle.equals(mainWindowHandle)) {
		        driver.switchTo().window(handle); // Switch to the pop-up window
		        break;
		    }
		}
		
		
		
		
		
		

	}

}
